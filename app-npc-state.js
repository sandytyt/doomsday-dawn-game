'use strict';

/**
 * 宣告 npcStates 結構與函式骨架
 */

function createNpcStateSkeleton(npcName) {
  // 階段2 將實作初始化邏輯
}

function correctNpcStateOnRejoin(npcName, currentTurn) {
  // 階段2 將實作離隊超過 15 回合的校正邏輯
}

function clearNpcStateOnDeath(npcName) {
  // 階段6 將實作死亡清除邏輯
}
