'use strict';

/**
 * 宣告轉移 UI 的 DOM 快取變數與狀態
 */
var transferDom = {
  // 階段3 將實作 DOM 綁定
};

var transferState = {
  mode: 'none', // 可為 'none', 'stash', 'vehicle', 'npc'
  sourceLocation: null,
  targetLocation: null,
  selectedItem: null
};
